import XCTest
@testable import EnhencerEvents

final class EnhencerEventsTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(EnhencerEvents().text, "Testing Enhencer Events SDK...")
    }
}
